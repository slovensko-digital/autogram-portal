service: autogram-portal

image: sd/autogram-portal

servers:
  web:
    options:
      "add-host": host.docker.internal:host-gateway

proxy:
  ssl: true
  app_port: 3000

env:
  secret:
    - SECRET_KEY_BASE
    - DATABASE_URL
    - WEBHOOK_PRIVATE_KEY
    - GOOGLE_CLIENT_ID
    - GOOGLE_CLIENT_SECRET
    - SMTP_PASSWORD
    - ALTCHA_HMAC_KEY
    - ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY
    - ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY
    - ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT

aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f

accessories:
  ag_service:
    image: registry.dev.slovensko.digital/sd/ag-service
    roles:
      - web
    proxy:
      ssl: true
      host: ag-service.dev.slovensko.digital
      app_port: 7200
      healthcheck:
        path: /info


registry:
  server: registry.dev.slovensko.digital
  username:
    - KAMAL_REGISTRY_USERNAME
  password:
    - KAMAL_REGISTRY_PASSWORD

builder:
  arch: amd64
  remote: ssh://kamal@dev.slovensko.digital
  cache:
    type: registry
    options: mode=max,image-manifest=true,oci-mediatypes=true

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
#
# asset_path: /app/public/assets
