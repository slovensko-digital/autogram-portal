<p class="greeting"><%= t('devise.mailer.greeting', recipient: @resource.email) %></p>

<p><%= t('.instruction') %></p>

<%= link_to t('.action'), magic_link_url(@resource, @scope_name => {email: @resource.email, token: @token, remember_me: @remember_me}), class: 'button' %>

<p><%= t('.expiry_notice', expiry: Devise.passwordless_login_within.inspect.to_i) %></p>
