# Consent records for fixture users.
# User "one" has accepted both current policy versions.
# User "two" has not accepted anything yet.

# == Schema Information
#
# Table name: user_policy_consents
#
#  id             :bigint           not null, primary key
#  accepted_at    :datetime         not null
#  ip_address     :string
#  policy_type    :string           not null
#  policy_version :string           not null
#  source         :string           not null
#  user_agent     :string
#  created_at     :datetime         not null
#  updated_at     :datetime         not null
#  user_id        :bigint           not null
#
# Indexes
#
#  index_user_policy_consents_on_user_id                  (user_id)
#  index_user_policy_consents_on_user_id_and_accepted_at  (user_id,accepted_at)
#  index_user_policy_consents_on_user_policy_version      (user_id,policy_type,policy_version)
#
# Foreign Keys
#
#  fk_rails_...  (user_id => users.id)
#
terms_one:
  user: one
  policy_type: terms
  policy_version: "1"
  source: email_signup
  accepted_at: <%= Time.now.utc.iso8601 %>

privacy_one:
  user: one
  policy_type: privacy
  policy_version: "1"
  source: email_signup
  accepted_at: <%= Time.now.utc.iso8601 %>
